# mise configuration
# https://mise.jdx.dev/configuration.html

[tools]
# Default global tools - override per-project with .mise.toml
node = "lts"
python = "latest"

# Add more tools as needed:
# go = "latest"
# rust = "latest"
# java = "21"
